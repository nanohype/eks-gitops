apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ing-internet
  namespace: workloads
  annotations:
    alb.ingress.kubernetes.io/scheme: internet-facing
    alb.ingress.kubernetes.io/subnets: subnet-pub-a,subnet-pub-b
spec:
  rules:
    - host: b.example.com
