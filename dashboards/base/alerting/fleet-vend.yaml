# eks-fleet vend pipeline — Grafana-managed SLO / health alert rules. The SLO is
# "99% of provider-opentofu reconciles succeed over 30d" (budget 0.01); a failing
# reconcile is a tofu plan/apply error on a cluster Workspace. Self-contained over
# the provider's controller-runtime metrics, which reach AMP once the
# provider-opentofu pod carries the prometheus.io/scrape annotation (eks-fleet
# vend-provider-scrape). Per-cluster vend inventory/readiness (Cluster + Workspace
# CR state) is out of scope for this group — it needs the kube-state-metrics
# customResourceState config extended to the Cluster and Workspace kinds, which
# these rules do not read.
#
# The burn rules carry two extra factors, both load-bearing:
#
#   clamp_min(<denominator>, 0.001)  — the divide-by-zero guard, at the org's own
#     sliDenominatorFloor. Floored at 1 instead, the denominator is pinned at 1
#     reconcile/s whenever the provider is quieter than that, which it always is,
#     and the ratio degrades into an absolute error rate needing ~518 failures an
#     hour to page. provider-opentofu failing 100% of reconciles at 0.1/s
#     computes a burn of 10 against a 14.4 threshold: cluster vending completely
#     broken, alert silent.
#
#   (rate(...) > bool 0.0167)  — a minimum-traffic gate, ~1 reconcile/minute over
#     the long window, so a ratio taken over near-zero traffic cannot page.
#
# When reconciles stop entirely the gate silences the burn rules, which is the
# condition FleetVendProviderAbsent below exists to catch.
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaAlertRuleGroup
metadata:
  name: fleet-vend-slo
spec:
  instanceSelector:
    matchLabels:
      dashboards: external
  folderRef: slo-alerts
  interval: 1m
  rules:
    - uid: fleet-vend-fast-burn
      title: FleetVendReconcileFastBurn
      condition: B
      for: 2m
      noDataState: OK
      execErrState: Error
      isPaused: false
      labels:
        severity: page
        service: eks-fleet
        component: vend
      annotations:
        runbook_url: https://github.com/nanohype/eks-fleet/blob/main/docs/runbooks/vend-failure.md
        summary: fleet vend reconcile errors burning the budget fast (2% in 1h)
        description: >
          provider-opentofu reconcile errors exceed 14.4x the 99% success
          objective over both the 1h and 5m windows. Vends are failing — inspect
          the cluster Workspaces' Synced condition (kubectl get workspace) for the
          tofu/apply error, and the provider-opentofu pod logs.
      data:
        - refId: A
          datasourceUid: managed-prometheus
          relativeTimeRange: {from: 3600, to: 0}
          model:
            refId: A
            datasource: {type: prometheus, uid: managed-prometheus}
            editorMode: code
            instant: true
            range: false
            intervalMs: 1000
            maxDataPoints: 43200
            expr: "(sum(rate(controller_runtime_reconcile_errors_total{namespace=\"crossplane-system\"}[1h])) / clamp_min(sum(rate(controller_runtime_reconcile_total{namespace=\"crossplane-system\"}[1h])), 0.001) / 0.01 > bool 14.4) * (sum(rate(controller_runtime_reconcile_errors_total{namespace=\"crossplane-system\"}[5m])) / clamp_min(sum(rate(controller_runtime_reconcile_total{namespace=\"crossplane-system\"}[5m])), 0.001) / 0.01 > bool 14.4) * (sum(rate(controller_runtime_reconcile_total{namespace=\"crossplane-system\"}[1h])) > bool 0.0167)"
        - refId: B
          datasourceUid: __expr__
          relativeTimeRange: {from: 3600, to: 0}
          model:
            refId: B
            type: threshold
            datasource: {type: __expr__, uid: __expr__}
            expression: A
            conditions:
              - type: query
                evaluator: {type: gt, params: [0]}
                operator: {type: and}
                query: {params: [A]}
                reducer: {type: last, params: []}
    - uid: fleet-vend-slow-burn
      title: FleetVendReconcileSlowBurn
      condition: B
      for: 15m
      noDataState: OK
      execErrState: Error
      isPaused: false
      labels:
        severity: page
        service: eks-fleet
        component: vend
      annotations:
        runbook_url: https://github.com/nanohype/eks-fleet/blob/main/docs/runbooks/vend-failure.md
        summary: fleet vend reconcile errors burning the budget (5% in 6h)
        description: >
          provider-opentofu reconcile errors exceed 6x the 99% success objective
          over both the 6h and 30m windows — a sustained vend failure. Investigate
          the failing Workspaces before the budget is exhausted.
      data:
        - refId: A
          datasourceUid: managed-prometheus
          relativeTimeRange: {from: 21600, to: 0}
          model:
            refId: A
            datasource: {type: prometheus, uid: managed-prometheus}
            editorMode: code
            instant: true
            range: false
            intervalMs: 1000
            maxDataPoints: 43200
            expr: "(sum(rate(controller_runtime_reconcile_errors_total{namespace=\"crossplane-system\"}[6h])) / clamp_min(sum(rate(controller_runtime_reconcile_total{namespace=\"crossplane-system\"}[6h])), 0.001) / 0.01 > bool 6) * (sum(rate(controller_runtime_reconcile_errors_total{namespace=\"crossplane-system\"}[30m])) / clamp_min(sum(rate(controller_runtime_reconcile_total{namespace=\"crossplane-system\"}[30m])), 0.001) / 0.01 > bool 6) * (sum(rate(controller_runtime_reconcile_total{namespace=\"crossplane-system\"}[6h])) > bool 0.0167)"
        - refId: B
          datasourceUid: __expr__
          relativeTimeRange: {from: 21600, to: 0}
          model:
            refId: B
            type: threshold
            datasource: {type: __expr__, uid: __expr__}
            expression: A
            conditions:
              - type: query
                evaluator: {type: gt, params: [0]}
                operator: {type: and}
                query: {params: [A]}
                reducer: {type: last, params: []}
    - uid: fleet-vend-provider-absent
      title: FleetVendProviderAbsent
      condition: B
      for: 5m
      noDataState: OK
      execErrState: Error
      isPaused: false
      labels:
        severity: page
        service: eks-fleet
        component: vend
      annotations:
        runbook_url: https://github.com/nanohype/eks-fleet/blob/main/docs/runbooks/vend-failure.md
        summary: provider-opentofu reconcile metrics absent — the vend provider is down or unscraped
        description: >
          No controller-runtime reconcile metrics from crossplane-system. The
          provider-opentofu deployment is down, crashlooping, or the scrape broke —
          the hub cannot vend or reconcile clusters. Check the provider pod and the
          provider's Healthy condition.
      data:
        - refId: A
          datasourceUid: managed-prometheus
          relativeTimeRange: {from: 600, to: 0}
          model:
            refId: A
            datasource: {type: prometheus, uid: managed-prometheus}
            editorMode: code
            instant: true
            range: false
            intervalMs: 1000
            maxDataPoints: 43200
            expr: "absent(controller_runtime_reconcile_total{namespace=\"crossplane-system\"})"
        - refId: B
          datasourceUid: __expr__
          relativeTimeRange: {from: 600, to: 0}
          model:
            refId: B
            type: threshold
            datasource: {type: __expr__, uid: __expr__}
            expression: A
            conditions:
              - type: query
                evaluator: {type: gt, params: [0]}
                operator: {type: and}
                query: {params: [A]}
                reducer: {type: last, params: []}
