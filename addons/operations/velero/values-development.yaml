# Velero - Development overrides
# Velero is not deployed to development — the addons-velero ApplicationSet
# selects `environment NotIn [hub, development]` because development has no
# backup bucket. This file exists to satisfy the per-env delta convention and is
# never loaded.
