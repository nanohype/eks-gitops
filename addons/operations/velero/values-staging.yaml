# Velero - Staging overrides
# The backup bucket and region are injected by the addons-velero ApplicationSet
# from the cluster Secret (annotation `velero/backup-bucket` + `region` label),
# so staging carries no storage-location delta of its own.
