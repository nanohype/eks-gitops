# Grafana Alloy - Production overrides
# extraEnv (incl. AWS_REGION + CLUSTER_NAME) is injected per cluster by the
# addons-alloy ApplicationSet.

alloy:
  resources:
    requests:
      cpu: 100m
      memory: 256Mi
    limits:
      cpu: 500m
      memory: 512Mi
