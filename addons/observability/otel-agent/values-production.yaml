# OpenTelemetry Collector agent — Production overrides.
# The only per-cluster input (K8S_NODE_NAME, via the downward API) is set in base
# values; the agent has no per-environment delta. Base defaults stand.
