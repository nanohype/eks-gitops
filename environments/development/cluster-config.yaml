apiVersion: v1
kind: ConfigMap
metadata:
  name: cluster-config
  namespace: argocd
  labels:
    environment: development
data:
  # Cluster identification
  environment: "development"
  cluster_name: "development-platform"

  # AWS configuration
  region: "us-west-2"
