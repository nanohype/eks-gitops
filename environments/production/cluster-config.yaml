apiVersion: v1
kind: ConfigMap
metadata:
  name: cluster-config
  namespace: argocd
  labels:
    environment: production
data:
  # Cluster identification
  environment: "production"
  cluster_name: "production-platform"

  # AWS configuration
  region: "us-west-2"
