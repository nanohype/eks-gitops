apiVersion: v1
kind: ConfigMap
metadata:
  name: cluster-config
  namespace: argocd
  labels:
    environment: staging
data:
  # Cluster identification
  environment: "staging"
  provider: "aws"
  cluster_name: "staging-platform"

  # AWS configuration
  region: "us-west-2"
